import { ConfirmaPasswordValidatorDirective } from './confirma-password-validator.directive';

describe('ConfirmaPasswordValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmaPasswordValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
